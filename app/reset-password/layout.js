export const metadata = {
  title: "Reset Password | SoSign",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
